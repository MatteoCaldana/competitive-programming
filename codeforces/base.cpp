#include <bits/stdc++.h>
using namespace std;
using ul = size_t;
using ll = long long;
using vector<ul> = vul;
using vector<ll> = vll;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    // -------------------------------------------------------------------------


    // -------------------------------------------------------------------------
    return 0;
}