#include <bits/stdc++.h>

using namespace std;
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int size;
    string str;
    cin >> size >> str;
    cout << str;

    return 0;
}